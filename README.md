# Financial Analysis Tool

A backend API for fetching, storing, and analyzing financial market data from public APIs.

## Getting Started

```bash
uv sync
uv run uvicorn main:app --reload
```

The API will be available at `http://localhost:8000`.
